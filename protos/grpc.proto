syntax = "proto3";
package protos;

message GrpcMessage {
    int32 cid = 1;
    int32 cmd = 2;
    int32 n = 3;
    int32 t = 4;
    bytes data = 5;
}

service GrpcService {
    rpc MService(stream GrpcMessage) returns (stream GrpcMessage){}
}